---
name: digest-preview
description: Preview the morning digest email without sending (dry run with --force to bypass routine-day check)
disable-model-invocation: true
---

Preview the digest email without sending:

```bash
bash send_digest.sh --dry-run --force
```
